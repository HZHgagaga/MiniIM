    本文件夹存储各个用户的好友消息记录，命名格式为"用户用户名"+"好友用户名".txt，这样每个用户每个好友都有一个单独的文件存储通信记录。
每次打开相应好友的对话框都会将文件的内容读入。
    此外，还有以"用户用户名"+"好友用户名"+"-".txt命名的文件，此文件将通过判断其是否含有"我"字符来确定是否应该在好友列表中将该好友标
记为有新的消息未读。由于水平有限，所以双方打开对话框时该文件也会出现"我"字符，为了防止出现消息未读标记，关闭对话框时将清除该字符。
